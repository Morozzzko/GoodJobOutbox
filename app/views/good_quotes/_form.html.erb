<%= form_with(model: good_quote) do |form| %>
  <% if good_quote.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(good_quote.errors.count, "error") %> prohibited this good_quote from being saved:</h2>

      <ul>
        <% good_quote.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :text, style: "display: block" %>
    <%= form.text_area :text %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
